{
  "name": "trace-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Trace competitive backend — per-level best-time + fewest-backtracks leaderboards on Cloudflare Workers + D1.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute trace-arena --remote --file=schema.sql",
    "schema:local": "wrangler d1 execute trace-arena --local --file=schema.sql",
    "test": "node test/e2e.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
